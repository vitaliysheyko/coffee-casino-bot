services:
  - type: web
    name: coffee-casino-bot
    runtime: docker
    dockerCommand: python main.py
    healthCheckPath: /health
    envVars:
      - key: BOT_TOKEN
        sync: false
    plan: free
